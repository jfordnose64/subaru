{
  "private": true,
  "name": "subaru",
  "scripts": {
    "start": "browser-sync start --server public --files public",
    "deploy": "netlify deploy --prod --dir=public",
    "setup-deploy": "app-app --deploy && yarn deploy",
    "postdeploy": "echo 'Your site was deployed to https://subaru-jacksonford.netlify.com'"
  },
  "devDependencies": {
    "browser-sync": "^2.26.7",
    "sass": "^1.23.0-module.beta.1",
    "stylelint": "^10.1.0",
    "stylelint-config-standard": "^18.3.0"
  },
  "homepage": "https://subaru-jacksonford.netlify.com",
  "dependencies": {
    "node-sass": "^7.0.0"
  }
}
